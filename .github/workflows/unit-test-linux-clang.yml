name: Unit Tests (Linux Clang)
on:
  pull_request:
  push:
    branches:
    - 'master'
    paths:
    - '.github/workflows/unit-test-linux-clang.yml'
    - 'Makefile'
    - 'tests/**'
    - 'third_party/unity/**'
    - 'src/common/**.c'
    - 'src/common/**.h'
permissions:
  contents: read
jobs:
  unit-test:
    name: Unit Test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Run unit tests
      run: make test CC=clang
